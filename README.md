# API REST CON MONGO

## INSTRUCCIONES
1. instalar dependencias
```bash
npm install
```

2. Renombre el archivo .env.template a .env e inicializalas con los valores adecuados
```

DB_URI=cadena de conexión a la bbadd de mongo
PORT=puerto

```